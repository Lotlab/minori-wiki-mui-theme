<li>
    <% config.theme.side_links.forEach(function(side) { %>
    <strong><%= side.name %></strong>
    <ul>
        <% side.links.forEach(function(link) { %>
            <li><a href="<%= link.link %>"><%= link.name %></a></li>
        <% }) %>
    </ul>
    <% }) %>
</li>
